/*document.getElementById("enviar").addEventListener('click', function mostrarMensaje() {
    // Capturamos el valor que escribió el usuario en el input
    let nombre = document.getElementById("nuevo-nombre").value;

    // Creamos el mensaje personalizado con el nombre
    let mensaje = "¡Hola " + nombre + "!";

    // Mostramos el mensaje dentro del párrafo con id "mensaje-respuesta"
    document.getElementById("mensaje-respuesta").innerHTML = mensaje;
})*/